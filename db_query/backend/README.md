# db_query backend

FastAPI backend for the smart database query tool. See the [project README](../README.md) for setup and usage.

Quick start:

```bash
uv sync --extra dev
uv run uvicorn app.main:app --reload --port 8000
```
